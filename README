A little Chess Application with an easter egg

LEG={@: ?column, #: ?row, 1-8: rows, a-g: columns}

TODOs:
        # 1. Check for STALEMATE & CHECKMATE, INSUFFICIENT MATERIAL, 50-MOVE, THREEFOLD REPETITION (no.)
        # 2. Bot (0, 1 done)
        # 3. Castling King-Side & Queen-Side
        # 4. En passant
        # 5. tests (done, continue adding)
        # 6. add turn indicator
        # 7. add counts of taken pieces (done)
        # 8. add turn info in fen (done)

5/30
-checkmate does not distinguish between a real checkmate and a stalemate
(king cannot move but is not in check) (and no valid moves)
-show the number of eaten pieces for each category

5/28
DEADLINE: 5/30 Sat (next session (work at home) )
1 > Add pawn stopping on @-8/1
2 > Add Pawn Promotion
3 > Add Castling King Side & Queen Side (King Side first)

5/26
change print in eval board pos to logger.debug
check how to enable the logger during tests
fix issue with valid moves for pawn that goes off board



5/14
modified board.go_to to move the piece (before the piece was moved only in non lw mode)
this fixed issue with get threatened pieces.
noted that board has row 0 instead of just 1-8. check if that is caused by the undo move
noted also that in bot1 when black queen moved to 4,7 we should have only the queen threatened but instead we get also 2 pawns.
this is because the white queen is in a wrong position and also the board is messed up with white piece in rows 1 and 0 instead of 1 and 2.

=>add an assertion in all places that modify the board.state to make sure that the board has no 0 key
=> add test for threatened piece detection
=>fix test for undo move that does not really test the undo

=>after all of that re-debug the bot1 test


5/9
finish level one bot
-DONE
fix filter
-king or knight

5/7/26
level one bot makes the move that gives it the most score

5/3/2026
make 0 & 1 bots. (see below)
add difficulty selection.



4/30
fixed valid moves of king
added test for that
started bot:
-level 0:random move
-level 1:
  -create function that evaluates new score (the higher numbers first) (for each color)
  -loop over pieces and move to each valid move, pick move that brings the highest score board
  eval: sum the value of each piece W_V and B_V value(white)=W_V/(W_V+B_V)


4/25/2026
-checkmate detection
-checkmate popup (prep)
    a. show winner
- add ctc & download history

TOD: refactor the UI and infinite drawing loop:
 -add a UI state class
   -contains a list of UI abjects to draw (list because it needs to support drawing order (layers))
   -add a draw method that loops over the list and calls the draw method of each item in the list
   -current UI states: INIT, GAME, END, CHECKMATE
   -current transitions:
     start at INIT
     INIT -play click-> GAME
     INIT -quit--> END
     INIT -horse-->INIT
     GAME -back---> INIT
     GAME --checkmate-->CHECKMATE
     CHECKMATE --ok-->INIT



4/18
-fixed bug in test of valid moves (did not re-get the opponent pieces after each piece move
 (example: one of the valid moves to consider filtering ate an opponent piece)
-work on adding the state information (turn, jain, count of pieces)


3/21
       take care of 1.2
