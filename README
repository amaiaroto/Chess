A little Chess Application with an easter egg


TODOs:
        # 1. Check for STALEMATE & CHECKMATE, INSUFFICIENT MATERIAL, 50-MOVE, THREEFOLD REPETITION
        # 2. Bot
        # 3. Castling King-Side & Queen-Side
        # 4. En passant
        # 5. tests (done, continue adding)
        # 6. add turn indicator
        # 7. add counts of taken pieces
        # 8. add turn info in fen (done)

5/9
finish level one bot
-DONE
fix filter
-king or knight

5/7/26
level one bot makes the move that gives it the most score

5/3/2026
make 0 & 1 bots. (see below)
add difficulty selection.



4/30
fixed valid moves of king
added test for that
started bot:
-level 0:random move
-level 1:
  -create function that evaluates new score (the higher numbers first) (for each color)
  -loop over pieces and move to each valid move, pick move that brings the highest score board
  eval: sum the value of each piece W_V and B_V value(white)=W_V/(W_V+B_V)


4/25/2026
-checkmate detection
-checkmate popup (prep)
    a. show winner
- add ctc & download history

TOD: refactor the UI and infinite drawing loop:
 -add a UI state class
   -contains a list of UI abjects to draw (list because it needs to support drawing order (layers))
   -add a draw method that loops over the list and calls the draw method of each item in the list
   -current UI states: INIT, GAME, END, CHECKMATE
   -current transitions:
     start at INIT
     INIT -play click-> GAME
     INIT -quit--> END
     INIT -horse-->INIT
     GAME -back---> INIT
     GAME --checkmate-->CHECKMATE
     CHECKMATE --ok-->INIT



4/18
-fixed bug in test of valid moves (did not re-get the opponent pieces after each piece move
 (example: one of the valid moves to consider filtering ate an opponent piece)
-work on adding the state information (turn, jain, count of pieces)


3/21
       take care of 1.2
